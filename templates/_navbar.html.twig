<nav class="navbar navbar-expand-lg navbar-light bg-mainColor ">
	<div class="container-fluid">
		<div class="d-flex flex-row align-items-center">
			<img id="logo" src="{{ asset('build/images/Capture.png') }}" alt="logo Wild Séries">
			<a class="navbar-brand brand p-0 font-weight-bold" href="{{ path('app_index')}}">Wild Series</a>
		</div>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0 w-100 justify-content-end align-items-center">
				<li class="nav-item">

					<a href="{{ path('program_index') }}">
						{{ 'series_all'|trans }}</a>


				</li>

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle text-textColor" href="{{ path('category_index') }}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						{{ 'categories'|trans }}
					</a>

					<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
						{% if categories is defined %}
							{% for category in categories %}
								<li>
									<a class="dropdown-item" href="{{ path('category_show', {categoryName: category.name }) }}">{{category.name|trans}}</a>
								</li>
							{% endfor %}
							<li><hr class="dropdown-divider"></li>
						{% endif %}
						<li>
							<a class="dropdown-item" href="{{ path('category_index') }}">{{ 'all_categories'|trans }}</a>
						</li>
					</ul>
				</li>

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle text-textColor" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						{{ 'actors'|trans }}
					</a>

					<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
						{% if actors is defined %}
							{% for actor in actors|slice(0,9) %}
								<li>
									<a class="dropdown-item" href="{{ path('app_actor_show', {id: actor.id }) }}">{{actor.name}}</a>
								</li>
								{% else %}
							{% endfor %}
							<li><hr class="dropdown-divider"></li>
						{% endif %}
						<li>
							<a class="dropdown-item" href="{{ path('app_actor_index') }}">{{ 'all_actors'|trans }}</a>
						</li>
					</ul>
				</li>

				<li class="nav-item ">
					<a class="nav-link text-textColor" href="{{ path('app_user_fav') }}">{{ 'favourites'|trans }}</a>
				</li>

				<li class="nav-item">
					{% if app.user %}
						<a href="{{ path('app_logout') }}">
							{{ 'account.sign_out'|trans }}</a>
					{% else %}
						<a href={{path('app_login')}}>{{ 'account.sign_in'|trans }}</a>
					{% endif %}
				</li>

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle text-textColor" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						{{ 'language'|trans }}</a>
					<ul class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown-language">
						<li>
							<a class="dropdown-item" href="{{ path('app_index', {_locale: 'en'}) }}">EN</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_index', {_locale: 'fr'}) }}">FR</a>
						</li>
					</ul>
				</li>
			</ul>
		</div>
	</div>
</nav>
