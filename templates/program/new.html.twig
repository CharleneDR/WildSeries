{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	-
	{{ 'series_create'|trans }}
{% endblock %}

{% block body %}
	<div class="d-flex flex-row justify-content-center mt-5">
		<img class=" me-5" src="{{ asset('build/images/categoryCut.png') }}">
		<div class="pimpedForm ms-5">
			<h1>{{ 'series_create'|trans }}</h1>

			{{ include('program/_form.html.twig') }}
		</div>
	</div>

	<div class="mb-2 fixed-bottom">
		<a href="{{ path('program_index') }}">
			<img src="{{ asset('build/images/previous.png') }}" id="returnLogo">
			{{ 'back'|trans }}
		</a>
	</div>

{% endblock %}
