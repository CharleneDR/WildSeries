{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	- Modifier une série
{% endblock %}

{% block body %}
	<h1>Modifier la série
		{{program.title}}</h1>

	{{ include('program/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('program_index') }}">back to list</a>

	{{ include('program/_delete_form.html.twig') }}
{% endblock %}
